# =============================================================================
# FlowSat requirements.
# Tested with Python 3.10 on Linux + CUDA 12.1, A100 / A5000.
# Install PyTorch first (matching your CUDA), then everything else.
#
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# =============================================================================

# --- Core ML stack -----------------------------------------------------------
torch>=2.3.0,<2.6
torchvision>=0.18.0
numpy>=1.24,<2.1
einops>=0.7.0

# --- HuggingFace ecosystem ---------------------------------------------------
# Sana, DC-AE VAE, EMAModel, schedulers
diffusers>=0.30.0,<0.36
# Gemma-2 (decoder LLM text encoder), CLIP for eval, tokenizers
transformers>=4.45.0,<4.50
# Cache layer; older versions before 0.26 had `local_files_only` issues we hit
huggingface-hub>=0.24,<0.30
# Tokenizer fast backends
tokenizers>=0.20

# --- Multi-GPU training & utilities -----------------------------------------
accelerate>=0.30.0
safetensors>=0.4.0

# --- Image I/O (FMoW TIFFs use rasterio for fast tile reads) ----------------
rasterio>=1.3.0
Pillow>=10.0.0
tifffile>=2024.1.0

# --- Data ingestion ---------------------------------------------------------
# Used by extract_rsicd_full.py and any HF-dataset loaders
datasets>=2.18.0
pyarrow>=14.0.0
pandas>=2.0.0

# --- Logging & progress -----------------------------------------------------
tqdm>=4.66.0
wandb>=0.16.0
tensorboard>=2.15.0

# --- Optional: evaluation metrics -------------------------------------------
# FID, IS, LPIPS, SSIM, CLIP score. Pin to a version that has all five.
torchmetrics>=1.3.0,<1.6
scipy>=1.11.0           # FID backend
lpips>=0.1.4            # AlexNet weights cache

# --- Optional: speed/quality of life ----------------------------------------
# 8-bit AdamW reduces optimiser state memory ~4x (useful on A5000 24 GB).
# Comment out if you do not want bitsandbytes.
bitsandbytes>=0.43.0; platform_system == "Linux"
